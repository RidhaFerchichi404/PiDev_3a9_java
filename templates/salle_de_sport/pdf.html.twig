<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ salle_de_sport.nom }} - Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 20px;
        }

        h1, h2, h3 {
            color: #1E90FF;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .details {
            margin-bottom: 20px;
        }

        .equipments {
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f4f4f4;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{ salle_de_sport.nom }}</h1>
        <p>Details of the gym and its equipment</p>
    </div>

    <div class="details">
        <h2>Gym Details</h2>
        <p><strong>Zone:</strong> {{ salle_de_sport.zone }}</p>
        <p><strong>Owner:</strong> {{ salle_de_sport.idUser }}</p>
        <p><strong>Total Equipments:</strong> {{ equipements|length }}</p>
    </div>

    <div class="equipments">
        <h2>Equipments</h2>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                   
                    <th>Last Verification</th>
                    <th>Next Verification</th>
                </tr>
            </thead>
            <tbody>
                {% for equipement in equipements %}
                    <tr>
                        <td>{{ equipement.nom }}</td>
                        <td>{{ equipement.derniereVerification|date('d/m/Y') }}</td>
                        <td>{{ equipement.prochaineVerification|date('d/m/Y') }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>