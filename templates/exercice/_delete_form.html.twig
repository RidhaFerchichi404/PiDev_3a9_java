<form method="post" action="{{ path('app_exercice_delete', {'id': exercice.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet exercice?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.id) }}">
    <button class="btn btn-danger btn-sm">
        <i class="ri-delete-bin-line"></i> Supprimer
    </button>
</form>
